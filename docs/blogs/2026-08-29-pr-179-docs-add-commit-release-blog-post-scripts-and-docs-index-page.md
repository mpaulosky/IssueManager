---
post_title: "docs: add commit/release blog-post scripts and docs index page"
author1: mpaulosky
post_slug: "v0.0.22-pr-179"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.0.22
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.0.22 from PR #179."
post_date: "2026-08-29"
---
## docs: add commit/release blog-post scripts and docs index page

- **Release tag:** `v0.0.22`
- **Source PR:** [#179](https://github.com/mpaulosky/IssueManager/pull/179)

### PR description

## Summary

- Adds `scripts/New-BlogPost.ps1` — generates a Markdown blog post from a commit (or a commit range, e.g. `HEAD~3..HEAD`) and stages it in `docs/blogs/` so it ships with the same push.
- Adds `scripts/New-ReleaseBlogPost.ps1` — generates a Markdown blog post from a GitHub release's notes (`-Tag <tag>`, or `-All` to backfill).
- Backfills `docs/blogs/` with a post for every existing GitHub release (v0.0.1 → v0.0.19, plus v0.0.9-108).
- Adds `docs/index.html` — a modern-styled static page mirroring the root README plus tables of blog posts and release documents, with progressive-enhancement JS that refreshes both tables live from the GitHub API when served over http(s).

Closes #178

## Test plan

- [x] `New-BlogPost.ps1` tested in single-ref and range (`HEAD~3..HEAD`) modes
- [x] `New-ReleaseBlogPost.ps1 -All` backfilled all 20 releases without error
- [x] Pre-push gate (markdown lint + full test suite) passed
- [ ] Open `docs/index.html` in a browser and confirm the tables render correctly

🤖 Generated with [Claude Code](https://claude.com/claude-code)

